# MuEvent

Simple static site project.

Files included:
- Home.html
- css/style.css

Setup

1. Install Git and (optionally) the GitHub CLI `gh`.
2. To create remote and push automatically, install and authenticate `gh`.

License: add one if desired.
